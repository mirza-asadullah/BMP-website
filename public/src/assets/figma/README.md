Place exported Figma hero images here if you want to import them into the bundle.

Currently the `Hero` component loads placeholder images from `/Home_images/` (public folder).
To move images into the source tree, copy them here and update imports in `Hero.tsx`.
