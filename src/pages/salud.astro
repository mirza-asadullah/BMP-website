---
// Página Salud: clonada de /chat con contenido específico de Salud.
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import HomeHero from '../components/home/HomeHero.astro';
import FeatureSlider from '../components/FeatureSlider.astro';
---

<Layout title="BeMind Power - Salud" description="BeMind Power: vive saludable con los tuyos. Comparte recetas, retos y planes de alimentación." bodyClass="salud-page">
  <div class="salud-page-wrap relative min-h-[100dvh] w-full">
    <Header />

    <!-- Layout Desktop: usa HomeHero con video y phone de Salud, slider inicia en Salud Mental (index 7) -->
    <div class="salud-desktop-layout">
      <HomeHero
        videoSrc="/videos/salud.mp4"
        phoneSrc="/images/smartphonesalud.png"
        titleLine1="Vive saludable"
        titleLine2="Con los tuyos."
        subtitle="Comparte recetas, retos saludables y planes de alimentación en tu red."
        ctaText="Ver Más"
        ctaHref="/salud"
        titleLine1Key="salud.hero.titleLine1"
        titleLine2Key="salud.hero.titleLine2"
        subtitleKey="salud.hero.subtitle"
        ctaKey="salud.hero.cta"
      >
        <FeatureSlider initialIndex={7} />
      </HomeHero>
    </div>

    <!-- Layout Mobile: estructura separada para mejor control -->
    <div class="salud-mobile-layout">
      <!-- Video de fondo -->
      <div class="salud-mobile-bg">
        <video
          class="salud-mobile-video"
          muted
          loop
          playsinline
          preload="auto"
        >
          <source src="/videos/salud.mp4" type="video/mp4" />
        </video>
        <div class="salud-mobile-overlay"></div>
        <div class="salud-mobile-glow"></div>
      </div>

      <!-- Contenido superior: título + botones -->
      <div class="salud-mobile-content">
        <div class="salud-mobile-title">
          <h1 class="salud-title">
            <span class="salud-title-line1" data-i18n="salud.hero.titleLine1">Vive saludable</span>
            <span class="salud-title-line2" data-i18n="salud.hero.titleLine2">Con los tuyos.</span>
          </h1>
          <p class="salud-subtitle" data-i18n="salud.hero.subtitle">Comparte recetas, retos saludables y planes de alimentación en tu red.</p>
        </div>

        <div class="salud-mobile-buttons">
          <a href="#slider" class="salud-cta" data-i18n="salud.hero.cta">Ver Más</a>
          <div class="salud-badges">
            <a href="#" class="salud-store-btn" aria-label="Download on the App Store">
              <img src="/images/app-store-badge.svg" alt="Download on the App Store" />
            </a>
            <a href="#" class="salud-store-btn" aria-label="Get it on Google Play">
              <img src="/images/google-play-badge.svg" alt="Get it on Google Play" />
            </a>
          </div>
        </div>
      </div>

      <!-- Slider abajo, inicia en Salud Mental (index 7) -->
      <div class="salud-mobile-slider">
        <FeatureSlider initialIndex={7} />
      </div>
    </div>
  </div>
</Layout>

<style is:global>
  body.salud-page {
    background: #000000;
    overflow-x: hidden;
  }

  /* Desktop: mostrar layout normal, ocultar mobile */
  .salud-desktop-layout {
    display: block;
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .salud-mobile-layout {
    display: none;
  }

  /* ===== MOBILE + TABLETS TÁCTILES ===== */
  @media (max-width: 768px), (hover: none) and (pointer: coarse) and (min-width: 769px) and (max-width: 1366px) {
    body.salud-page {
      overflow-x: hidden;
      overscroll-behavior: none;
    }

    .salud-desktop-layout {
      display: none;
    }

    /* Layout principal: altura fija al viewport, sin scroll */
    .salud-mobile-layout {
      display: flex;
      flex-direction: column;
      position: relative;
      width: 100%;
      height: 100dvh;
      height: 100vh;
      overflow: clip;
      box-sizing: border-box;
      padding-top: calc(70px + env(safe-area-inset-top));
      padding-bottom: max(1rem, env(safe-area-inset-bottom));
    }

    /* Video de fondo */
    .salud-mobile-bg {
      position: fixed;
      inset: 0;
      z-index: 0;
      overflow: hidden;
    }

    .salud-mobile-video {
      position: absolute;
      top: 50%;
      left: 50%;
      min-width: 100%;
      min-height: 100%;
      width: auto;
      height: auto;
      transform: translate(-50%, -50%);
      object-fit: cover;
      object-position: center;
      opacity: 0.85;
    }

    .salud-mobile-overlay {
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.3);
      z-index: 1;
    }

    .salud-mobile-glow {
      position: absolute;
      top: 40%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 80vw;
      height: 80vw;
      background: radial-gradient(
        circle,
        rgba(40, 54, 233, 0.89) 0%,
        rgba(40, 54, 233, 0.5) 40%,
        transparent 70%
      );
      filter: blur(198px);
      z-index: 2;
    }

    /* === CONTENIDO SUPERIOR (textos + botones) — 60% del espacio === */
    .salud-mobile-content {
      position: relative;
      z-index: 10;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      flex: 6;
      min-height: 0;
      padding: 0 5%;
      gap: clamp(1rem, 2.5vh, 1.5rem);
    }

    /* Título */
    .salud-mobile-title {
      text-align: center;
      width: 100%;
    }

    .salud-title {
      color: #fff;
      display: flex;
      flex-direction: column;
      gap: 0;
      margin: 0 0 clamp(0.3rem, 1vh, 0.6rem) 0;
      line-height: 1.1;
    }

    .salud-title-line1 {
      font-family: 'OPTIFutura-ExtraBlackCond', 'Inter', sans-serif;
      font-size: clamp(24px, 7vw, 40px);
      font-weight: 900;
      letter-spacing: 0.01em;
    }

    .salud-title-line2 {
      font-family: 'Neometric', 'Inter', sans-serif;
      font-size: clamp(14px, 4vw, 24px);
      font-weight: 700;
      letter-spacing: 0.01em;
    }

    .salud-subtitle {
      color: #fff;
      font-family: 'Neometric', 'Inter', sans-serif;
      font-size: clamp(11px, 2.8vw, 14px);
      font-weight: 400;
      line-height: 1.3;
      margin: 0;
      max-width: 92%;
      margin-left: auto;
      margin-right: auto;
    }

    /* Botones */
    .salud-mobile-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: clamp(0.4rem, 0.8vh, 0.6rem);
      width: 100%;
    }

    .salud-cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: clamp(42px, 5.5vh, 50px);
      padding: 0 2rem;
      width: min(280px, 72vw);
      background: #1929ff;
      color: #fff;
      font-family: 'OPTIFutura-ExtraBlackCond', 'Inter', sans-serif;
      font-weight: 900;
      font-size: clamp(14px, 3.8vw, 16px);
      letter-spacing: 0.02em;
      border-radius: 999px;
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .salud-cta:hover,
    .salud-cta:active {
      transform: scale(1.02);
      box-shadow: 0 4px 24px rgba(25, 41, 255, 0.5);
    }

    .salud-badges {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: clamp(0.3rem, 0.6vh, 0.5rem);
    }

    .salud-store-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: min(260px, 68vw);
      height: clamp(42px, 5.5vh, 50px);
      padding: 0 18px;
      background: rgba(0, 0, 0, 0.85);
      border: 1px solid rgba(255, 255, 255, 0.12);
      border-radius: 50px;
      box-shadow: 0 4px 14px rgba(55, 27, 190, 0.25), 0 -4px 14px rgba(25, 41, 255, 0.2);
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .salud-store-btn:hover,
    .salud-store-btn:active {
      transform: scale(1.02);
      box-shadow: 0 6px 20px rgba(55, 27, 190, 0.35);
    }

    .salud-store-btn img {
      height: clamp(24px, 3.8vh, 32px);
      width: auto;
      max-width: 100%;
      object-fit: contain;
    }

    /* === SLIDER ABAJO — 40% del espacio === */
    .salud-mobile-slider {
      position: relative;
      z-index: 5;
      width: 100%;
      flex: 4;
      min-height: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 2%;
      overflow: visible;
    }

    .salud-mobile-slider .feature-slider {
      width: 100%;
      height: 100%;
      min-height: 0 !important;
      flex: 1 1 0%;
      overflow: visible;
    }

    .salud-mobile-slider .slider-wrapper {
      padding: 0;
      height: 100%;
      min-height: 0;
      flex: 1 1 0%;
      overflow: visible;
    }

    .salud-mobile-slider .slider-container {
      height: 100%;
      min-height: 0 !important;
      flex: 1 1 0%;
      overflow: visible;
    }

    .salud-mobile-slider .slider-track {
      overflow: visible;
    }

    /* Ocultar flechas de navegación en móvil */
    .salud-mobile-slider .slider-nav {
      display: none;
    }
  }

  /* ===== TABLETS TÁCTILES (iPad, etc.) ===== */
  @media (hover: none) and (pointer: coarse) and (min-width: 769px) and (max-width: 1366px) {
    .salud-mobile-layout {
      padding-top: calc(90px + env(safe-area-inset-top));
    }

    .salud-mobile-title {
      max-width: 720px;
    }

    .salud-cta {
      width: min(520px, 60vw);
    }

    .salud-store-btn {
      width: min(520px, 60vw);
    }

  }

  /* ===== TABLETS PEQUEÑAS / MÓVILES GRANDES (601-768px) ===== */
  @media (max-width: 768px) and (min-width: 601px) {
    .salud-mobile-layout {
      padding-top: calc(80px + env(safe-area-inset-top));
    }

    .salud-title-line1 {
      font-size: clamp(34px, 5.5vw, 44px);
    }

    .salud-title-line2 {
      font-size: clamp(19px, 3.2vw, 26px);
    }

    .salud-subtitle {
      font-size: clamp(13px, 2vw, 16px);
    }

    .salud-cta {
      width: min(300px, 55vw);
      height: 52px;
      font-size: 16px;
    }

    .salud-store-btn {
      width: min(280px, 52vw);
      height: 52px;
    }

    .salud-store-btn img {
      height: 32px;
    }
  }

  /* ===== MÓVILES PEQUEÑOS (<=374px) - iPhone SE ===== */
  @media (max-width: 374px) {
    .salud-mobile-layout {
      padding-top: calc(62px + env(safe-area-inset-top));
    }

    .salud-mobile-content {
      padding-left: 4%;
      padding-right: 4%;
    }

    .salud-title-line1 {
      font-size: clamp(22px, 6.5vw, 28px);
    }

    .salud-title-line2 {
      font-size: clamp(13px, 3.8vw, 16px);
    }

    .salud-subtitle {
      font-size: 11px;
      max-width: 90%;
    }

    .salud-cta {
      width: min(230px, 70vw);
      height: 44px;
      font-size: 14px;
      padding: 0 1.5rem;
    }

    .salud-store-btn {
      width: min(210px, 66vw);
      height: 44px;
      padding: 0 14px;
    }

    .salud-store-btn img {
      height: 26px;
    }

  }

  /* ===== PANTALLAS MUY CORTAS ===== */
  @media (max-width: 768px) and (max-height: 650px) {
    .salud-mobile-layout {
      padding-top: calc(60px + env(safe-area-inset-top));
    }

    .salud-title-line1 {
      font-size: clamp(22px, 6.5vw, 30px);
    }

    .salud-title-line2 {
      font-size: clamp(13px, 3.5vw, 17px);
    }

    .salud-subtitle {
      font-size: 11px;
    }

    .salud-cta {
      height: 40px;
      font-size: 13px;
    }

    .salud-store-btn {
      height: 40px;
    }

    .salud-store-btn img {
      height: 22px;
    }

  }

  /* /salud móvil: centrar botón de asistencia verticalmente */
  @media (max-width: 640px) {
    body.salud-page .support-container {
      top: 50%;
      bottom: auto;
      left: max(16px, calc(env(safe-area-inset-left) + 16px));
      transform: translateY(-50%);
    }

    body.salud-page .support-tooltip {
      position: relative;
    }
  }

  /* ===== LANDSCAPE ===== */
  @media (max-width: 768px) and (max-height: 500px) and (orientation: landscape) {
    .salud-mobile-layout {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .salud-mobile-content {
      flex: 1;
      min-width: 50%;
      padding: 60px 2% 0.4rem 2%;
      gap: 0.25rem;
    }

    .salud-mobile-title {
      padding: 0;
    }

    .salud-title {
      margin-bottom: 0.3rem;
    }

    .salud-title-line1 {
      font-size: clamp(18px, 4.5vw, 26px);
    }

    .salud-title-line2 {
      font-size: clamp(11px, 2.5vw, 16px);
    }

    .salud-subtitle {
      font-size: 10px;
    }

    .salud-mobile-buttons {
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.3rem;
    }

    .salud-badges {
      flex-direction: row;
      gap: 0.3rem;
    }

    .salud-cta {
      height: 36px;
      width: auto;
      min-width: 130px;
      font-size: 11px;
    }

    .salud-store-btn {
      height: 36px;
      width: auto;
      min-width: 125px;
    }

    .salud-store-btn img {
      height: 18px;
    }

    .salud-mobile-slider {
      width: 100%;
      min-height: 150px;
      max-height: 200px;
    }
  }
</style>

<script>
  // Optimizar reproducción de video en mobile
  function initSaludMobileVideo(): void {
    const video = document.querySelector('.salud-mobile-video') as HTMLVideoElement;
    if (!video) return;

    const playVideo = () => {
      video.play().catch(() => {
        document.addEventListener('touchstart', () => {
          video.play();
        }, { once: true });
      });
    };

    if (video.readyState >= 2) {
      playVideo();
    } else {
      video.addEventListener('loadeddata', playVideo, { once: true });
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initSaludMobileVideo);
  } else {
    initSaludMobileVideo();
  }
</script>
