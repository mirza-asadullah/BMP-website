---
// Página Apps: clonada de /chat con contenido específico de Apps.
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import HomeHero from '../components/home/HomeHero.astro';
import FeatureSlider from '../components/FeatureSlider.astro';
---

<Layout title="BeMind Power - Apps" description="BeMind Power: una sola puerta a cientos de soluciones con tecnología inmersiva." bodyClass="apps-page">
  <div class="apps-page-wrap relative min-h-[100dvh] w-full">
    <Header currentPath="/apps" />

    <!-- Layout Desktop: usa HomeHero con video y phone de Apps, slider inicia en Apps (index 3) -->
    <div class="apps-desktop-layout">
      <HomeHero
        videoSrc="/videos/apps.mp4"
        phoneSrc="/images/SmartphoneApps.png"
        titleLine1="Todo empieza"
        titleLine2="con una aplicación"
        subtitle="Una sola puerta a cientos de soluciones. Accede a todas las apps que necesitas desde un solo lugar."
        ctaText="Ver Más"
        ctaHref="/apps"
        titleLine1Key="apps.hero.titleLine1"
        titleLine2Key="apps.hero.titleLine2"
        subtitleKey="apps.hero.subtitle"
        ctaKey="apps.hero.cta"
      >
        <FeatureSlider initialIndex={3} />
      </HomeHero>
    </div>

    <!-- Layout Mobile: estructura separada para mejor control -->
    <div class="apps-mobile-layout">
      <!-- Video de fondo -->
      <div class="apps-mobile-bg">
        <video
          class="apps-mobile-video"
          muted
          loop
          playsinline
          preload="auto"
        >
          <source src="/videos/apps.mp4" type="video/mp4" />
        </video>
        <div class="apps-mobile-overlay"></div>
        <div class="apps-mobile-glow"></div>
      </div>

      <!-- Contenido superior: título + botones -->
      <div class="apps-mobile-content">
        <div class="apps-mobile-title">
          <h1 class="apps-title">
            <span class="apps-title-line1" data-i18n="apps.hero.titleLine1">Todo empieza</span>
            <span class="apps-title-line2" data-i18n="apps.hero.titleLine2">con una aplicación</span>
          </h1>
          <p class="apps-subtitle" data-i18n="apps.hero.subtitle">Una sola puerta a cientos de soluciones. Accede a todas las apps que necesitas desde un solo lugar.</p>
        </div>

        <div class="apps-mobile-buttons">
          <a href="#slider" class="apps-cta" data-i18n="apps.hero.cta">Ver Más</a>
          <div class="apps-badges">
            <a href="#" class="apps-store-btn" aria-label="Download on the App Store">
              <img src="/images/app-store-badge.svg" alt="Download on the App Store" />
            </a>
            <a href="#" class="apps-store-btn" aria-label="Get it on Google Play">
              <img src="/images/google-play-badge.svg" alt="Get it on Google Play" />
            </a>
          </div>
        </div>
      </div>

      <!-- Slider abajo, inicia en Apps (index 3) -->
      <div class="apps-mobile-slider">
        <FeatureSlider initialIndex={3} />
      </div>
    </div>
  </div>
</Layout>

<style is:global>
  body.apps-page {
    background: #000000;
    overflow-x: hidden;
  }

  /* Desktop: mostrar layout normal, ocultar mobile */
  .apps-desktop-layout {
    display: block;
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .apps-mobile-layout {
    display: none;
  }

  /* ===== MOBILE + TABLETS TÁCTILES ===== */
  @media (max-width: 768px), (hover: none) and (pointer: coarse) and (min-width: 769px) and (max-width: 1366px) {
    body.apps-page {
      overflow-x: hidden;
      overscroll-behavior: none;
    }

    .apps-desktop-layout {
      display: none;
    }

    /* Layout principal: altura fija al viewport, sin scroll */
    .apps-mobile-layout {
      display: flex;
      flex-direction: column;
      position: relative;
      width: 100%;
      height: 100dvh;
      height: 100vh;
      overflow: clip;
      box-sizing: border-box;
      padding-top: calc(70px + env(safe-area-inset-top));
      padding-bottom: max(1rem, env(safe-area-inset-bottom));
    }

    /* Video de fondo */
    .apps-mobile-bg {
      position: fixed;
      inset: 0;
      z-index: 0;
      overflow: hidden;
    }

    .apps-mobile-video {
      position: absolute;
      top: 50%;
      left: 50%;
      min-width: 100%;
      min-height: 100%;
      width: auto;
      height: auto;
      transform: translate(-50%, -50%);
      object-fit: cover;
      object-position: center;
      opacity: 0.85;
    }

    .apps-mobile-overlay {
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.3);
      z-index: 1;
    }

    .apps-mobile-glow {
      position: absolute;
      top: 40%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 80vw;
      height: 80vw;
      background: radial-gradient(
        circle,
        rgba(40, 54, 233, 0.89) 0%,
        rgba(40, 54, 233, 0.5) 40%,
        transparent 70%
      );
      filter: blur(198px);
      z-index: 2;
    }

    /* === CONTENIDO SUPERIOR (textos + botones) — 60% del espacio === */
    .apps-mobile-content {
      position: relative;
      z-index: 10;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      flex: 6;
      min-height: 0;
      padding: 0 5%;
      gap: clamp(1rem, 2.5vh, 1.5rem);
    }

    /* Título */
    .apps-mobile-title {
      text-align: center;
      width: 100%;
    }

    .apps-title {
      color: #fff;
      display: flex;
      flex-direction: column;
      gap: 0;
      margin: 0 0 clamp(0.3rem, 1vh, 0.6rem) 0;
      line-height: 1.1;
    }

    .apps-title-line1 {
      font-family: 'OPTIFutura-ExtraBlackCond', 'Inter', sans-serif;
      font-size: clamp(24px, 7vw, 40px);
      font-weight: 900;
      letter-spacing: 0.01em;
    }

    .apps-title-line2 {
      font-family: 'Neometric', 'Inter', sans-serif;
      font-size: clamp(14px, 4vw, 24px);
      font-weight: 700;
      letter-spacing: 0.01em;
    }

    .apps-subtitle {
      color: #fff;
      font-family: 'Neometric', 'Inter', sans-serif;
      font-size: clamp(11px, 2.8vw, 14px);
      font-weight: 400;
      line-height: 1.3;
      margin: 0;
      max-width: 92%;
      margin-left: auto;
      margin-right: auto;
    }

    /* Botones */
    .apps-mobile-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: clamp(0.4rem, 0.8vh, 0.6rem);
      width: 100%;
    }

    .apps-cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: clamp(42px, 5.5vh, 50px);
      padding: 0 2rem;
      width: min(280px, 72vw);
      background: #1929ff;
      color: #fff;
      font-family: 'OPTIFutura-ExtraBlackCond', 'Inter', sans-serif;
      font-weight: 900;
      font-size: clamp(14px, 3.8vw, 16px);
      letter-spacing: 0.02em;
      border-radius: 999px;
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .apps-cta:hover,
    .apps-cta:active {
      transform: scale(1.02);
      box-shadow: 0 4px 24px rgba(25, 41, 255, 0.5);
    }

    .apps-badges {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: clamp(0.3rem, 0.6vh, 0.5rem);
    }

    .apps-store-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: min(260px, 68vw);
      height: clamp(42px, 5.5vh, 50px);
      padding: 0 18px;
      background: rgba(0, 0, 0, 0.85);
      border: 1px solid rgba(255, 255, 255, 0.12);
      border-radius: 50px;
      box-shadow: 0 4px 14px rgba(55, 27, 190, 0.25), 0 -4px 14px rgba(25, 41, 255, 0.2);
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .apps-store-btn:hover,
    .apps-store-btn:active {
      transform: scale(1.02);
      box-shadow: 0 6px 20px rgba(55, 27, 190, 0.35);
    }

    .apps-store-btn img {
      height: clamp(24px, 3.8vh, 32px);
      width: auto;
      max-width: 100%;
      object-fit: contain;
    }

    /* === SLIDER ABAJO — 40% del espacio === */
    .apps-mobile-slider {
      position: relative;
      z-index: 5;
      width: 100%;
      flex: 4;
      min-height: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 2%;
      overflow: visible;
    }

    .apps-mobile-slider .feature-slider {
      width: 100%;
      height: 100%;
      min-height: 0 !important;
      flex: 1 1 0%;
      overflow: visible;
    }

    .apps-mobile-slider .slider-wrapper {
      padding: 0;
      height: 100%;
      min-height: 0;
      flex: 1 1 0%;
      overflow: visible;
    }

    .apps-mobile-slider .slider-container {
      height: 100%;
      min-height: 0 !important;
      flex: 1 1 0%;
      overflow: visible;
    }

    .apps-mobile-slider .slider-track {
      overflow: visible;
    }

    /* Ocultar flechas de navegación en móvil */
    .apps-mobile-slider .slider-nav {
      display: none;
    }
  }

  /* ===== TABLETS TÁCTILES (iPad, etc.) ===== */
  @media (hover: none) and (pointer: coarse) and (min-width: 769px) and (max-width: 1366px) {
    .apps-mobile-layout {
      padding-top: calc(90px + env(safe-area-inset-top));
    }

    .apps-mobile-title {
      max-width: 720px;
    }

    .apps-cta {
      width: min(520px, 60vw);
    }

    .apps-store-btn {
      width: min(520px, 60vw);
    }

  }

  /* ===== TABLETS PEQUEÑAS / MÓVILES GRANDES (601-768px) ===== */
  @media (max-width: 768px) and (min-width: 601px) {
    .apps-mobile-layout {
      padding-top: calc(80px + env(safe-area-inset-top));
    }

    .apps-title-line1 {
      font-size: clamp(34px, 5.5vw, 44px);
    }

    .apps-title-line2 {
      font-size: clamp(19px, 3.2vw, 26px);
    }

    .apps-subtitle {
      font-size: clamp(13px, 2vw, 16px);
    }

    .apps-cta {
      width: min(300px, 55vw);
      height: 52px;
      font-size: 16px;
    }

    .apps-store-btn {
      width: min(280px, 52vw);
      height: 52px;
    }

    .apps-store-btn img {
      height: 32px;
    }
  }

  /* ===== MÓVILES PEQUEÑOS (<=374px) - iPhone SE ===== */
  @media (max-width: 374px) {
    .apps-mobile-layout {
      padding-top: calc(62px + env(safe-area-inset-top));
    }

    .apps-mobile-content {
      padding-left: 4%;
      padding-right: 4%;
    }

    .apps-title-line1 {
      font-size: clamp(22px, 6.5vw, 28px);
    }

    .apps-title-line2 {
      font-size: clamp(13px, 3.8vw, 16px);
    }

    .apps-subtitle {
      font-size: 11px;
      max-width: 90%;
    }

    .apps-cta {
      width: min(230px, 70vw);
      height: 44px;
      font-size: 14px;
      padding: 0 1.5rem;
    }

    .apps-store-btn {
      width: min(210px, 66vw);
      height: 44px;
      padding: 0 14px;
    }

    .apps-store-btn img {
      height: 26px;
    }

  }

  /* ===== PANTALLAS MUY CORTAS ===== */
  @media (max-width: 768px) and (max-height: 650px) {
    .apps-mobile-layout {
      padding-top: calc(60px + env(safe-area-inset-top));
    }

    .apps-title-line1 {
      font-size: clamp(22px, 6.5vw, 30px);
    }

    .apps-title-line2 {
      font-size: clamp(13px, 3.5vw, 17px);
    }

    .apps-subtitle {
      font-size: 11px;
    }

    .apps-cta {
      height: 40px;
      font-size: 13px;
    }

    .apps-store-btn {
      height: 40px;
    }

    .apps-store-btn img {
      height: 22px;
    }

  }

  /* /apps móvil: centrar botón de asistencia verticalmente */
  @media (max-width: 640px) {
    body.apps-page .support-container {
      top: 50%;
      bottom: auto;
      left: max(16px, calc(env(safe-area-inset-left) + 16px));
      transform: translateY(-50%);
    }

    body.apps-page .support-tooltip {
      position: relative;
    }
  }

  /* ===== LANDSCAPE ===== */
  @media (max-width: 768px) and (max-height: 500px) and (orientation: landscape) {
    .apps-mobile-layout {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .apps-mobile-content {
      flex: 1;
      min-width: 50%;
      padding: 60px 2% 0.4rem 2%;
      gap: 0.25rem;
    }

    .apps-mobile-title {
      padding: 0;
    }

    .apps-title {
      margin-bottom: 0.3rem;
    }

    .apps-title-line1 {
      font-size: clamp(18px, 4.5vw, 26px);
    }

    .apps-title-line2 {
      font-size: clamp(11px, 2.5vw, 16px);
    }

    .apps-subtitle {
      font-size: 10px;
    }

    .apps-mobile-buttons {
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.3rem;
    }

    .apps-badges {
      flex-direction: row;
      gap: 0.3rem;
    }

    .apps-cta {
      height: 36px;
      width: auto;
      min-width: 130px;
      font-size: 11px;
    }

    .apps-store-btn {
      height: 36px;
      width: auto;
      min-width: 125px;
    }

    .apps-store-btn img {
      height: 18px;
    }

    .apps-mobile-slider {
      width: 100%;
      min-height: 150px;
      max-height: 200px;
    }
  }
</style>

<script>
  // Optimizar reproducción de video en mobile
  function initAppsMobileVideo(): void {
    const video = document.querySelector('.apps-mobile-video') as HTMLVideoElement;
    if (!video) return;

    const playVideo = () => {
      video.play().catch(() => {
        document.addEventListener('touchstart', () => {
          video.play();
        }, { once: true });
      });
    };

    if (video.readyState >= 2) {
      playVideo();
    } else {
      video.addEventListener('loadeddata', playVideo, { once: true });
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initAppsMobileVideo);
  } else {
    initAppsMobileVideo();
  }
</script>
