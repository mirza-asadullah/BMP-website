---
// Página Música: clonada de /chat con contenido específico de Música.
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import HomeHero from '../components/home/HomeHero.astro';
import FeatureSlider from '../components/FeatureSlider.astro';
---

<Layout title="BeMind Power - Música" description="BeMind Power: tus canciones, tus playlists y nuevos descubrimientos. Todo en un solo lugar." bodyClass="musica-page">
  <div class="musica-page-wrap relative min-h-[100dvh] w-full">
    <Header currentPath="/musica" />

    <!-- Layout Desktop: usa HomeHero con video y phone de Música, slider inicia en Música (index 6) -->
    <div class="musica-desktop-layout">
      <HomeHero
        videoSrc="/videos/musica.mp4"
        phoneSrc="/images/musica.png"
        titleLine1="La música vive"
        titleLine2="Escúchala y comparte"
        subtitle="Tus canciones, tus playlists y nuevos descubrimientos. Todo en un solo lugar."
        ctaText="Ver Más"
        ctaHref="/musica"
        titleLine1Key="musica.hero.titleLine1"
        titleLine2Key="musica.hero.titleLine2"
        subtitleKey="musica.hero.subtitle"
        ctaKey="musica.hero.cta"
      >
        <FeatureSlider initialIndex={6} />
      </HomeHero>
    </div>

    <!-- Layout Mobile: estructura separada para mejor control -->
    <div class="musica-mobile-layout">
      <!-- Video de fondo -->
      <div class="musica-mobile-bg">
        <video
          class="musica-mobile-video"
          muted
          loop
          playsinline
          preload="auto"
        >
          <source src="/videos/musica.mp4" type="video/mp4" />
        </video>
        <div class="musica-mobile-overlay"></div>
        <div class="musica-mobile-glow"></div>
      </div>

      <!-- Contenido superior: título + botones -->
      <div class="musica-mobile-content">
        <div class="musica-mobile-title">
          <h1 class="musica-title">
            <span class="musica-title-line1" data-i18n="musica.hero.titleLine1">La música vive</span>
            <span class="musica-title-line2" data-i18n="musica.hero.titleLine2">Escúchala y comparte</span>
          </h1>
          <p class="musica-subtitle" data-i18n="musica.hero.subtitle">Tus canciones, tus playlists y nuevos descubrimientos. Todo en un solo lugar.</p>
        </div>

        <div class="musica-mobile-buttons">
          <a href="#slider" class="musica-cta" data-i18n="musica.hero.cta">Ver Más</a>
          <div class="musica-badges">
            <a href="#" class="musica-store-btn" aria-label="Download on the App Store">
              <img src="/images/app-store-badge.svg" alt="Download on the App Store" />
            </a>
            <a href="#" class="musica-store-btn" aria-label="Get it on Google Play">
              <img src="/images/google-play-badge.svg" alt="Get it on Google Play" />
            </a>
          </div>
        </div>
      </div>

      <!-- Slider abajo, inicia en Música (index 6) -->
      <div class="musica-mobile-slider">
        <FeatureSlider initialIndex={6} />
      </div>
    </div>
  </div>
</Layout>

<style is:global>
  body.musica-page {
    background: #000000;
    overflow-x: hidden;
  }

  .musica-desktop-layout {
    display: block;
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .musica-mobile-layout {
    display: none;
  }

  /* ===== MOBILE + TABLETS TÁCTILES ===== */
  @media (max-width: 768px), (hover: none) and (pointer: coarse) and (min-width: 769px) and (max-width: 1366px) {
    body.musica-page {
      overflow-x: hidden;
      overscroll-behavior: none;
    }

    .musica-desktop-layout {
      display: none;
    }

    /* Layout principal: altura fija al viewport, sin scroll */
    .musica-mobile-layout {
      display: flex;
      flex-direction: column;
      position: relative;
      width: 100%;
      height: 100dvh;
      height: 100vh;
      overflow: clip;
      box-sizing: border-box;
      padding-top: calc(70px + env(safe-area-inset-top));
      padding-bottom: max(1rem, env(safe-area-inset-bottom));
    }

    .musica-mobile-bg {
      position: fixed;
      inset: 0;
      z-index: 0;
      overflow: hidden;
    }

    .musica-mobile-video {
      position: absolute;
      top: 50%;
      left: 50%;
      min-width: 100%;
      min-height: 100%;
      width: auto;
      height: auto;
      transform: translate(-50%, -50%);
      object-fit: cover;
      object-position: center;
      opacity: 0.85;
    }

    .musica-mobile-overlay {
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.3);
      z-index: 1;
    }

    .musica-mobile-glow {
      position: absolute;
      top: 40%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 80vw;
      height: 80vw;
      background: radial-gradient(
        circle,
        rgba(40, 54, 233, 0.89) 0%,
        rgba(40, 54, 233, 0.5) 40%,
        transparent 70%
      );
      filter: blur(198px);
      z-index: 2;
    }

    /* === CONTENIDO SUPERIOR (textos + botones) — 60% del espacio === */
    .musica-mobile-content {
      position: relative;
      z-index: 10;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      flex: 6;
      min-height: 0;
      padding: 0 5%;
      gap: clamp(1rem, 2.5vh, 1.5rem);
    }

    .musica-mobile-title {
      text-align: center;
      width: 100%;
    }

    .musica-title {
      color: #fff;
      display: flex;
      flex-direction: column;
      gap: 0;
      margin: 0 0 clamp(0.3rem, 1vh, 0.6rem) 0;
      line-height: 1.1;
    }

    .musica-title-line1 {
      font-family: 'OPTIFutura-ExtraBlackCond', 'Inter', sans-serif;
      font-size: clamp(24px, 7vw, 40px);
      font-weight: 900;
      letter-spacing: 0.01em;
    }

    .musica-title-line2 {
      font-family: 'Neometric', 'Inter', sans-serif;
      font-size: clamp(14px, 4vw, 24px);
      font-weight: 700;
      letter-spacing: 0.01em;
    }

    .musica-subtitle {
      color: #fff;
      font-family: 'Neometric', 'Inter', sans-serif;
      font-size: clamp(11px, 2.8vw, 14px);
      font-weight: 400;
      line-height: 1.3;
      margin: 0;
      max-width: 92%;
      margin-left: auto;
      margin-right: auto;
    }

    .musica-mobile-buttons {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: clamp(0.4rem, 0.8vh, 0.6rem);
      width: 100%;
    }

    .musica-cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: clamp(42px, 5.5vh, 50px);
      padding: 0 2rem;
      width: min(280px, 72vw);
      background: #1929ff;
      color: #fff;
      font-family: 'OPTIFutura-ExtraBlackCond', 'Inter', sans-serif;
      font-weight: 900;
      font-size: clamp(14px, 3.8vw, 16px);
      letter-spacing: 0.02em;
      border-radius: 999px;
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .musica-cta:hover,
    .musica-cta:active {
      transform: scale(1.02);
      box-shadow: 0 4px 24px rgba(25, 41, 255, 0.5);
    }

    .musica-badges {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: clamp(0.3rem, 0.6vh, 0.5rem);
    }

    .musica-store-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: min(260px, 68vw);
      height: clamp(42px, 5.5vh, 50px);
      padding: 0 18px;
      background: rgba(0, 0, 0, 0.85);
      border: 1px solid rgba(255, 255, 255, 0.12);
      border-radius: 50px;
      box-shadow: 0 4px 14px rgba(55, 27, 190, 0.25), 0 -4px 14px rgba(25, 41, 255, 0.2);
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .musica-store-btn:hover,
    .musica-store-btn:active {
      transform: scale(1.02);
      box-shadow: 0 6px 20px rgba(55, 27, 190, 0.35);
    }

    .musica-store-btn img {
      height: clamp(24px, 3.8vh, 32px);
      width: auto;
      max-width: 100%;
      object-fit: contain;
    }

    /* === SLIDER ABAJO — 40% del espacio === */
    .musica-mobile-slider {
      position: relative;
      z-index: 5;
      width: 100%;
      flex: 4;
      min-height: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 2%;
      overflow: visible;
    }

    .musica-mobile-slider .feature-slider {
      width: 100%;
      height: 100%;
      min-height: 0 !important;
      flex: 1 1 0%;
      overflow: visible;
    }

    .musica-mobile-slider .slider-wrapper {
      padding: 0;
      height: 100%;
      min-height: 0;
      flex: 1 1 0%;
      overflow: visible;
    }

    .musica-mobile-slider .slider-container {
      height: 100%;
      min-height: 0 !important;
      flex: 1 1 0%;
      overflow: visible;
    }

    .musica-mobile-slider .slider-track {
      overflow: visible;
    }

    .musica-mobile-slider .slider-nav {
      display: none;
    }
  }

  /* ===== TABLETS TÁCTILES (iPad, etc.) ===== */
  @media (hover: none) and (pointer: coarse) and (min-width: 769px) and (max-width: 1366px) {
    .musica-mobile-layout {
      padding-top: calc(90px + env(safe-area-inset-top));
    }

    .musica-mobile-title {
      max-width: 720px;
    }

    .musica-cta {
      width: min(520px, 60vw);
    }

    .musica-store-btn {
      width: min(520px, 60vw);
    }

  }

  /* ===== TABLETS PEQUEÑAS / MÓVILES GRANDES (601-768px) ===== */
  @media (max-width: 768px) and (min-width: 601px) {
    .musica-mobile-layout {
      padding-top: calc(80px + env(safe-area-inset-top));
    }

    .musica-title-line1 {
      font-size: clamp(34px, 5.5vw, 44px);
    }

    .musica-title-line2 {
      font-size: clamp(19px, 3.2vw, 26px);
    }

    .musica-subtitle {
      font-size: clamp(13px, 2vw, 16px);
    }

    .musica-cta {
      width: min(300px, 55vw);
      height: 52px;
      font-size: 16px;
    }

    .musica-store-btn {
      width: min(280px, 52vw);
      height: 52px;
    }

    .musica-store-btn img {
      height: 32px;
    }
  }

  /* ===== MÓVILES PEQUEÑOS (<=374px) - iPhone SE ===== */
  @media (max-width: 374px) {
    .musica-mobile-layout {
      padding-top: calc(62px + env(safe-area-inset-top));
    }

    .musica-mobile-content {
      padding-left: 4%;
      padding-right: 4%;
    }

    .musica-title-line1 {
      font-size: clamp(22px, 6.5vw, 28px);
    }

    .musica-title-line2 {
      font-size: clamp(13px, 3.8vw, 16px);
    }

    .musica-subtitle {
      font-size: 11px;
      max-width: 90%;
    }

    .musica-cta {
      width: min(230px, 70vw);
      height: 44px;
      font-size: 14px;
      padding: 0 1.5rem;
    }

    .musica-store-btn {
      width: min(210px, 66vw);
      height: 44px;
      padding: 0 14px;
    }

    .musica-store-btn img {
      height: 26px;
    }

  }

  /* ===== PANTALLAS MUY CORTAS ===== */
  @media (max-width: 768px) and (max-height: 650px) {
    .musica-mobile-layout {
      padding-top: calc(60px + env(safe-area-inset-top));
    }

    .musica-title-line1 {
      font-size: clamp(22px, 6.5vw, 30px);
    }

    .musica-title-line2 {
      font-size: clamp(13px, 3.5vw, 17px);
    }

    .musica-subtitle {
      font-size: 11px;
    }

    .musica-cta {
      height: 40px;
      font-size: 13px;
    }

    .musica-store-btn {
      height: 40px;
    }

    .musica-store-btn img {
      height: 22px;
    }

  }

  /* /musica móvil: centrar botón de asistencia verticalmente */
  @media (max-width: 640px) {
    body.musica-page .support-container {
      top: 50%;
      bottom: auto;
      left: max(16px, calc(env(safe-area-inset-left) + 16px));
      transform: translateY(-50%);
    }

    body.musica-page .support-tooltip {
      position: relative;
    }
  }

  /* ===== LANDSCAPE ===== */
  @media (max-width: 768px) and (max-height: 500px) and (orientation: landscape) {
    .musica-mobile-layout {
      flex-direction: row;
      flex-wrap: wrap;
    }

    .musica-mobile-content {
      flex: 1;
      min-width: 50%;
      padding: 60px 2% 0.4rem 2%;
      gap: 0.25rem;
    }

    .musica-mobile-title {
      padding: 0;
    }

    .musica-title {
      margin-bottom: 0.3rem;
    }

    .musica-title-line1 {
      font-size: clamp(18px, 4.5vw, 26px);
    }

    .musica-title-line2 {
      font-size: clamp(11px, 2.5vw, 16px);
    }

    .musica-subtitle {
      font-size: 10px;
    }

    .musica-mobile-buttons {
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.3rem;
    }

    .musica-badges {
      flex-direction: row;
      gap: 0.3rem;
    }

    .musica-cta {
      height: 36px;
      width: auto;
      min-width: 130px;
      font-size: 11px;
    }

    .musica-store-btn {
      height: 36px;
      width: auto;
      min-width: 125px;
    }

    .musica-store-btn img {
      height: 18px;
    }

    .musica-mobile-slider {
      width: 100%;
      min-height: 150px;
      max-height: 200px;
    }
  }
</style>

<script>
  function initMusicaMobileVideo(): void {
    const video = document.querySelector('.musica-mobile-video') as HTMLVideoElement;
    if (!video) return;

    const playVideo = () => {
      video.play().catch(() => {
        document.addEventListener('touchstart', () => {
          video.play();
        }, { once: true });
      });
    };

    if (video.readyState >= 2) {
      playVideo();
    } else {
      video.addEventListener('loadeddata', playVideo, { once: true });
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initMusicaMobileVideo);
  } else {
    initMusicaMobileVideo();
  }
</script>
