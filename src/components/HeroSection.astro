---
import PixelCharacter from './PixelCharacter.astro';

interface Props {
  userName?: string;
}

const { userName = 'paula' } = Astro.props;
---

<section class="relative min-h-screen flex flex-col items-center justify-center px-6 pt-20">
  <!-- Decorative elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <!-- Gradient orbs -->
    <div class="absolute top-1/4 -left-32 w-64 h-64 bg-indigo-500/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-1/4 -right-32 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] 
                bg-gradient-radial from-indigo-500/5 to-transparent rounded-full"></div>
  </div>
  
  <div class="relative z-10 flex flex-col items-center text-center max-w-4xl mx-auto">
    <!-- Status badge -->
    <div class="hero-element opacity-0 mb-8">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full glass border border-indigo-500/20">
        <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
        <span class="text-sm text-white/70">Sistema activo</span>
      </div>
    </div>
    
    <!-- Main heading -->
    <h1 class="hero-element opacity-0 text-5xl md:text-7xl lg:text-8xl font-black tracking-tight mb-6">
      <span class="block text-white">Welcome back</span>
      <span class="block text-gradient">{userName}!</span>
    </h1>
    
    <!-- Subtitle -->
    <p class="hero-element opacity-0 text-lg md:text-xl text-white/60 max-w-xl mb-12">
      Tu espacio de poder mental está listo. Continúa donde lo dejaste.
    </p>
    
    <!-- Character -->
    <div class="hero-element opacity-0 w-48 h-48 md:w-64 md:h-64 mb-12">
      <PixelCharacter />
    </div>
    
    <!-- CTA Buttons -->
    <div class="hero-element opacity-0 flex flex-col sm:flex-row items-center gap-4">
      <button class="group relative px-8 py-4 bg-gradient-to-r from-indigo-500 to-purple-600 
                     rounded-2xl font-semibold text-white overflow-hidden
                     hover:shadow-lg hover:shadow-indigo-500/30 transition-all duration-300
                     hover:-translate-y-1">
        <span class="relative z-10">Comenzar sesión</span>
        <div class="absolute inset-0 bg-gradient-to-r from-indigo-600 to-purple-700 
                    opacity-0 group-hover:opacity-100 transition-opacity"></div>
      </button>
      
      <button class="px-8 py-4 rounded-2xl font-semibold text-white/80
                     glass hover:bg-white/10 transition-all duration-300
                     hover:-translate-y-1">
        Ver tutorial
      </button>
    </div>
  </div>
  
  <!-- Skip intro button -->
  <button 
    id="skip-intro"
    class="absolute bottom-8 right-8 text-white/40 hover:text-white/80 
           text-sm font-medium transition-colors duration-300
           flex items-center gap-2"
  >
    <span>Omitir Intro</span>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 5l7 7-7 7M5 5l7 7-7 7" />
    </svg>
  </button>
  
  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-white/30">
    <span class="text-xs uppercase tracking-widest">Scroll</span>
    <div class="w-6 h-10 rounded-full border-2 border-white/20 flex justify-center pt-2">
      <div class="w-1.5 h-3 bg-white/40 rounded-full animate-bounce"></div>
    </div>
  </div>
</section>

<script type="module" lang="ts">
  import { initHeroEffects } from '../scripts/hero';

  initHeroEffects();
</script>

<style>
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .bg-gradient-radial {
    background: radial-gradient(circle, var(--tw-gradient-from), var(--tw-gradient-to));
  }
</style>
