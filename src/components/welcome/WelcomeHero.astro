---
// Página inicial: hero compuesto por componentes pequeños e inspeccionables por separado.
import HeroBackground from './HeroBackground.astro';
import HeroLogoGeometric from './HeroLogoGeometric.astro';
import HeroContent from './HeroContent.astro';
import HeroIcons from './HeroIcons.astro';
import HeroPhone from './HeroPhone.astro';

interface Props {
  welcomeText?: string;
  mainTitle?: string;
  description?: string;
}

const { 
  welcomeText,
  mainTitle,
  description
} = Astro.props;
---

<div class="welcome-hero">
  <HeroBackground />

  <HeroContent 
    welcomeText={welcomeText}
    mainTitle={mainTitle}
    description={description}
  />

  <HeroPhone />
</div>

<style>
  .welcome-hero {
    position: relative;
    width: 100%;
    height: 100vh;
    min-height: 100dvh;
    overflow: hidden;
  }
</style>
